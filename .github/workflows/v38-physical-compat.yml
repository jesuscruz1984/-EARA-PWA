name: Agent v38 Physical Compatibility (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'v38 is archived. Use the v39 EP6 cellular/live workflow.'
