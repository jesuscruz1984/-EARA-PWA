name: EARA v42 Microphone Fix APK (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'v42 is archived. Use the EARA v43 continuous-stream microphone build.'
